name: Sigma CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pySigma
        run: pip install sigma-cli
      - name: Validate rules
        run: sigma validate detections/sigma --fail-on-warning
